# Handoff status

Nothing pending user testing.

Last completed (2026-07-18): downloads page (#45) — user-tested, merged, and
**live at https://paperhurts.github.io/doc-md/**. It self-updates from the
GitHub API on every published release; no redeploy ever needed.

On hold by user choice: #43 code signing — the workflow wiring is merged and
dormant. When ready, `docs/SIGNING.md` has the one-time Azure setup
(paperhurts-scoped: one $9.99/mo account signs all current and future apps).
The Azure identity-verification step is the long pole — start it first.
