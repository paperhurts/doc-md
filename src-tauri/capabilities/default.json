{
  "identifier": "default",
  "description": "Default capability set for doc-md",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-stdin-write",
    "shell:allow-spawn",
    "shell:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
